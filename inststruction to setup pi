# 1 and 2 for setup 3g device configuration.
1:sudo apt-get install usb-modeswitch
2:sudo leafpad /etc/usb_modeswitch.conf

#3 and 4 for install gps library for python and 5 for automatic start gps on boot, 6 for configure the RX and TX port to recive data.
3:sudo apt-get install gpsd
4:sudo apt-get install gpsd-clients
5:sudo dpkg-reconfigure gpsd
6:sudo leafpad /etc/inittab // comment the last line.

# 7 for emacs texteditor
7:sudo  apt-get intsall emacs

#8 virtual keyboard
8: sudo apt-get install matchbox-keyboard

# 9 for run tracker on everyy boot
9:sudo crontab -e (add the program to run on startup)

# 10 dev rule
ACTION=="add", SUBSYSTEM=="usb", ATTRS{idVendor}=="12d1", ATTRS{idProduct}=="1f01", RUN+="/usr/sbin/usb_modeswitch -v 0x12d1 -p 0x1f01 -M '55534243123456780000000000000a11062000000000000100000000000000'"
